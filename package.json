{
  "name": "diploma-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm install && npm run start --prefix server && npm run start --prefix client",
    "build": "npm install && npm run build --prefix client",
    "heroku-postbuild": "npm install --prefix client && npm run build --prefix client"
  },
  "dependencies": {
    "mongodb": "^6.7.0",
    "express": "^4.19.2",
    "mongoose": "^6.0.12",
    "cors": "^2.8.5",
    "axios": "^0.24.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3",
    "react-router-dom": "^5.3.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}